# Evaluating the Robustness of Pathology Foundation Models to a Feature Space Adversarial Attack

This repository contains the full implementation used to evaluate the adversarial robustness of three state-of-the-art Pathology Foundation Models (PFMs) — [**UNI**](https://www.nature.com/articles/s41591-024-02857-3), [**CONCH**](https://www.nature.com/articles/s41591-024-02856-4) and [**Virchow**](https://arxiv.org/abs/2309.07778) — against the task-agnostic feature-space adversarial attack proposed by [Pulfer et al. (2025)](https://ieeexplore.ieee.org/abstract/document/11147627), in both white-box and black-box scenarios, as well as Gaussian smoothing as a defensive strategy.

## Repository Structure

```
.
├── 01_feature_extraction.ipynb              # Feature extraction from clean and adversarial images with and without defense 
├── 02_adv_img_visualization.ipynb           # Adversarial image visualization
├── 03_robustness_eval.ipynb                 # Robustness evaluation through UMAP, cosine similarity and linear probing 
├── environment.yml                          # Conda environment specification
├── setup.sh                                 # Environment setup script
├── README.md
├── adv_images/                              # Adversarial images
│   └── <model_name>/
│       └── psnr_<XdB>/
│           └── <split>_batch_<NNNN>.pt
├── data/                                    # TCGA-UT dataset
├── features/                                # Feature embeddings
│   ├── clean/                               # Extracted from clean images, with and without Gaussian smoothing
│   │   └── <model_name>/
│   │       ├── <split>.h5
│   │       └── <split>_sigma<σ>.h5
│   └── adversarial/                         # Extracted from adversarial images, with and without Gaussian smoothing
│       └── <target_model_name>/
│           └── psnr_<XdB>/
│               ├── <split>_wb.h5
│               ├── <split>_wb_sigma<σ>.h5
│               └── <split>_transf_<source_model_name>.h5
├── model_weights/                           # PFM checkpoints
│   └── <model_name>.bin
├── results/
│   ├── embed_shift/                         # Cosine similarity results
│   ├── img_vis/                             # Image visualizations
│   ├── linear_probe/                        # Linear probe results
│   └── umap/                                # UMAP visualizations
└── utils/
    ├── __init__.py
    ├── constant.py                          # Global constants
    └── paths.py                             # Path management for models, features and adversarial images
```

> **Note:** `data/`, `adv_images/`, `features/` and `model_weights/` are included as empty directories. See [Dataset](#dataset) below to download TCGA-UT dataset, while all other contents are automatically downloaded or generated by running the notebooks in order (see [Pipeline](#pipeline) below).

## Setup

### 1. Clone the repository

```bash
git clone https://github.com/EmmaSalvagno/fsaa_pfms.git
cd fsaa_pfms
```

### 2. Set the base directory

The `BASE_DIR` variable in `utils/constant.py` serves as the root path for all project files and outputs. Update this path to point to wherever you cloned this repository locally:

```python
BASE_DIR = '/path/to/fsaa_pfms'
```

### 3. Create the Conda environment

Run `setup.sh` to create the Conda environment with all required dependencies, then activate it:

```bash
bash setup.sh
conda activate fsaa_pfms
```


## Dataset

Experiments are conducted on the **TCGA-UT** dataset, a publicly available collection of H&E-stained histopathology image patches spanning 31 human cancer types. This project uses a subset of five classes: bladder urothelial carcinoma, colon-rectum adenocarcinoma, prostate adenocarcinoma, skin cutaneous melanoma and thyroid carcinoma.

To download the dataset, follow the instructions on the [official Hugging Face page](https://huggingface.co/datasets/dakomura/tcga-ut):

```bash
git clone https://huggingface.co/datasets/dakomura/tcga-ut
cd tcga-ut
```

Move the `data/` folder from the cloned `tcga-ut` repository into the root of this project, keeping its internal structure unchanged:

```
data/
├── dataset_internal_train_part000.tar
├── dataset_internal_valid_part000.tar
├── dataset_internal_test_part000.tar
└── ...
```

## Pathology Foundation Model Weights

Access to each model must be requested and approved on Hugging Face before downloading:

- **UNI**: [request access](https://huggingface.co/MahmoodLab/UNI)
- **CONCH**: [request access](https://huggingface.co/MahmoodLab/CONCH)
- **Virchow**: [request access](https://huggingface.co/paige-ai/Virchow)

Once access is granted, enter your Hugging Face token when prompted in `01_feature_extraction.ipynb`, and the notebook will automatically download the checkpoints into `model_weights/`.


## Pipeline

The experimental pipeline is organized into three sequential notebooks:

1. **`01_feature_extraction.ipynb`** — Extracts features from clean images with and without Gaussian smoothing defense via `run_clean_feature_extraction()`, generates adversarial images via `run_fsaa()`, and extracts features from adversarial images via `run_adv_feature_extraction()`. Adversarial images are saved as PyTorch tensors in the `adv_images/` directory, while feature embeddings are stored as HDF5 files in `features/`.

2. **`02_adv_img_visualization.ipynb`** — Visualizes adversarial images and the effects of the Gaussian smoothing defense, saving the output to the `results/img_vis/` directory.

3. **`03_robustness_eval.ipynb`** — Runs the full evaluation protocol: feature space analysis through UMAP projections and cosine similarity, alongside representation separability assessment via linear probing. Outputs are saved to the `results/umap/`, `results/embed_shift/` and `results/linear_probe/` directories. 

## Author

Emma Salvagno — emma.salvagno@studio.unibo.it
